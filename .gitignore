# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Virtual environments
.venv/
venv/
ENV/
env/

# IDE settings
.vscode/
.idea/
*.swp
*.swo

# OS files
.DS_Store
Thumbs.db

# Game saves or temporary files
*.save
*.tmp
saves/
debug.dat
screenshots/
tmp/

# Balance/AI batch-run outputs (tools/arena_match.py runs)
tools/balance_matrix_*/
tools/personality_battery_*/

# Sprite pipeline scratch: raw generated sheets, tint checks, preview
# strips/GIFs, and processed intermediates. Keep these locally — the raw
# sources are the pipeline's INPUT and are not regenerable — but only the
# adopted sheet alongside them belongs in the repo.
_gen/

# Test cache
.pytest_cache/

# AI settings
.claude/

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
# PyInstaller / Inno Setup installer output
installer_output/
installer/app.ico
# User-local runtime files (settings + hotkey overrides)
settings.json
keybindings.json
profile.json
